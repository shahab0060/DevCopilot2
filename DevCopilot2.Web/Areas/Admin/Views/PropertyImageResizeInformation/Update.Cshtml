@inject IViewLocalizer Localizer
@inject IHtmlLocalizer<SharedResources> SharedLocalizer
@inject IHtmlLocalizer<EntitiesSharedResources> SharedEntitiesLocalizer
@using System
@using System.ComponentModel.DataAnnotations
@using DevCopilot2.Domain.DTOs.Properties
@model UpdatePropertyImageResizeInformationDto
@{
	ViewData["Title"] = $@"{SharedLocalizer.GetString("Update")} {SharedEntitiesLocalizer.GetString("PropertyImageResizeInformation")}";

}

<nav class="mb-3" aria-label="breadcrumb">
    <ol class="breadcrumb mb-0">

        <li class="breadcrumb-item"><a asp-area="Admin"
                                  asp-controller="Home"
                                  asp-action="Index" 
                                   title="@SharedLocalizer.GetString("Admin")">@SharedLocalizer.GetString("Admin")</a></li>
<li class="breadcrumb-item"><a asp-area="Admin"
                                  asp-controller="PropertyImageResizeInformation"
                                  asp-action="Index" 

                                asp-route-propertyIdId="@Model.PropertyId" title="@SharedEntitiesLocalizer.GetString("PropertyImageResizeInformation")">@SharedEntitiesLocalizer.GetString("PropertyImageResizeInformation")</a></li>

        <li class="breadcrumb-item active">@SharedLocalizer.GetString("Update") @SharedEntitiesLocalizer.GetString("PropertyImageResizeInformation")</li>
    </ol>   
</nav>
<form class="mb-9" method="post" 
                            asp-area="Admin" asp-controller="PropertyImageResizeInformation" asp-action="Update"
                            enctype="multipart/form-data">

    <div class="row justify-content-between align-items-end g-3 mb-5">
        <div class="col-12 col-sm-auto col-xl-8">
            <h2 class="mb-0">@SharedLocalizer.GetString("Update") @SharedEntitiesLocalizer.GetString("PropertyImageResizeInformation")</h2>
        </div>
        <div class="col-12 col-sm-auto col-xl-4">
            <div class="d-flex">
                                <button class="btn btn-primary px-5 w-100 text-nowrap">@SharedLocalizer.GetString("Update") @SharedEntitiesLocalizer.GetString("PropertyImageResizeInformation")</button>
            </div>
        </div>
    </div>
    <div class="row g-5">

                <input type="hidden" asp-for="@Model.Id" class=""
                       />
<input type="hidden" asp-for="@Model.PropertyId" class=""
                       />
<div class="col-sm-12 col-md-6">
    <div class="form-floating">
        <input class="form-control Title-input"  asp-for="@Model.Name"  placeholder="" />
        <label asp-for="@Model.Name"></label>
    </div>
    <span asp-validation-for="@Model.Name"></span>
</div>
<div class="col-sm-12 col-md-6">
    <div class="form-floating">
        <input class="form-control Others-input"  asp-for="@Model.Width" type="number" placeholder="" />
        <label asp-for="@Model.Width"></label>
    </div>
    <span asp-validation-for="@Model.Width"></span>
</div>
<div class="col-sm-12 col-md-6">
    <div class="form-floating">
        <input class="form-control Others-input"  asp-for="@Model.Height" type="number" placeholder="" />
        <label asp-for="@Model.Height"></label>
    </div>
    <span asp-validation-for="@Model.Height"></span>
</div>

    </div>
</form>

@section Scripts
{
	<script src="/Shared/JqueryValidation.min.js"></script>
	<script>
		$(document).ready(function (e) {
			$('.PropertyImageResizeInformationManagement').addClass('active');
            $('.PropertyImageResizeInformationManagement').addClass('show');
		});
	</script>
}
