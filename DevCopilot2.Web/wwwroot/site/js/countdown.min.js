"use strict";function Countdown(id,deadline){this.deadline=deadline,this.id=id;var self=this;if(self.clock=document.getElementById(self.id),null!==self.clock){self.daysSpan=self.clock.querySelector(".days"),self.hoursSpan=self.clock.querySelector(".hours"),self.minutesSpan=self.clock.querySelector(".minutes"),self.secondsSpan=self.clock.querySelector(".seconds"),updateClock();var timeinterval=setInterval(updateClock,1e3)}function updateClock(){var t=getTimeRemaining(self.deadline);self.daysSpan.innerHTML=t.days,self.hoursSpan.innerHTML=("0"+t.hours).slice(-2),self.minutesSpan.innerHTML=("0"+t.minutes).slice(-2),self.secondsSpan.innerHTML=("0"+t.seconds).slice(-2),t.total<=0&&clearInterval(timeinterval)}function getTimeRemaining(endtime){var t=Date.parse(endtime)-Date.parse(new Date),seconds=Math.floor(t/1e3%60),minutes=Math.floor(t/1e3/60%60),hours=Math.floor(t/36e5%24),days;return{total:t,days:Math.floor(t/864e5),hours:hours,minutes:minutes,seconds:seconds}}}